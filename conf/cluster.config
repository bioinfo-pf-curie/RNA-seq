/*
 * -------------------------------------------------
 *  Nextflow config file for your cluster
 * -------------------------------------------------
 */

process {
  executor = 'pbs'
  if (params.queue){
   queue = params.queue
  }
}
